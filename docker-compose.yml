services:
  app:
    build: .
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      DATA_FILE: /app/data/results.json
      DATABASE_PATH: /app/data/app.db
    ports:
      - "8080:8080"
    volumes:
      - app-data:/app/data

volumes:
  app-data:
